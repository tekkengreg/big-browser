{
  "name": "@tekkengreg/big-browser",
  "version": "1.0.0",
  "description": "Electron app loading url",
  "main": "src/main.js",
  "scripts": {
    "start": "electron ./src/main.js --no-sandbox",
    "build": "electron-builder",
    
    "generate-apps": "node generate-app.js all",
    "update-icons": "node generate-app.js update-icons",
    "add-app": "node generate-app.js add",
    "build-apps": "node generate-app.js build",
    "update-repo": "echo '📦 Mise à jour du repository...' && flatpak build-update-repo repo >/dev/null 2>&1 && echo '✅ Repository mis à jour avec succès!' && echo '📋 Applications disponibles:' && flatpak remote-ls local-repo | wc -l | xargs echo -n && echo ' applications'",
    "update-repo-verbose": "flatpak build-update-repo repo",
    "deploy": "pnpm run generate-apps && pnpm run update-repo && echo '🚀 Déploiement terminé!",
    "check-updates": "echo '🔍 Vérification des mises à jour disponibles...' && echo 'Applications installées:' && flatpak list --app --user | grep tekkengreg",
    
    "build:base": "flatpak-builder --repo=repo --force-clean build-dir-base manifests/app-template-simple-offline.yml",
    "install:base": "flatpak --user install repo com.tekkengreg.bigbrowser.Base",
    
    "build:flatpak:notion": "flatpak-builder --repo=repo --force-clean build-dir-notion manifests/app-template-simple-offline.yml",
    "build:flatpak:google": "flatpak-builder --repo=repo --force-clean build-dir-google manifests/app-template-simple-offline.yml",
    "build:flatpak:sketchup": "flatpak-builder --repo=repo --force-clean build-dir-sketchup manifests/app-template-simple-offline.yml",
    
    "build:flatpak:all:optimized": "pnpm build:flatpak:notion && pnpm build:flatpak:google && pnpm build:flatpak:sketchup",
    "build:flatpak:full": "pnpm build:base && pnpm install:base && pnpm build:flatpak:all:optimized",
    
    "build:flatpak:all": "pnpm build:flatpak && pnpm build:flatpak:google && pnpm build:flatpak:sketchup",
    "web:page": "http-server -p 8080 web"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "electron": "^35.5.1",
    "electron-builder": "^26.0.12",
    "http-server": "^14.1.1"
  },

  "pnpm": {
    "onlyBuiltDependencies": [
      "electron"
    ]
  },
 
  "packageManager": "pnpm@10.12.1+sha512.f0dda8580f0ee9481c5c79a1d927b9164f2c478e90992ad268bbb2465a736984391d6333d2c327913578b2804af33474ca554ba29c04a8b13060a717675ae3ac"
}
